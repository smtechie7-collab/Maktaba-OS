# Core Dependencies
weasyprint>=61.0
pydub>=0.25.1
PyQt6>=6.6.0
PyQt6-WebEngine>=6.6.0
Jinja2>=3.1.3
requests>=2.31.0

# Development & Testing
pytest>=8.0.0
black>=24.1.0
ruff>=0.2.0
pyinstaller>=6.0.0

# Database
# sqlite3 is built-in, no extra dependency needed unless using specific drivers.
