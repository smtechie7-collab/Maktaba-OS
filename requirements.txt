# Core Dependencies
weasyprint>=61.0
pydub>=0.25.1
PyQt6>=6.6.0
Jinja2>=3.1.3

# Development & Testing
pytest>=8.0.0
black>=24.1.0
ruff>=0.2.0

# Database
# sqlite3 is built-in, no extra dependency needed unless using specific drivers.
